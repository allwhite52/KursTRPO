<Window x:Class="KursTRPO.AddSuppliersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursTRPO"
        mc:Ignorable="d"
        Title="AddSuppliersWindow" Height="230" Width="330" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Название поставщика:" />
            <TextBox x:Name="NameSupplierTextBox" />

            <TextBlock Text="Телефон:" Margin="0,10,0,0" />
            <TextBox x:Name="PhoneSupplierTextBox" PreviewTextInput="PhoneSupplierTextBox_PreviewTextInput"/>

            <TextBlock Text="Адрес:" Margin="0,10,0,0" />
            <TextBox x:Name="AddressSupplierTextBox" />

            <Button x:Name="SaveButton" Content="Сохранить" Click="SaveButton_Click" Margin="0,20,0,0" />
        </StackPanel>
    </Grid>
</Window>
